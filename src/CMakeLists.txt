message("-- Processing: ${CMAKE_CURRENT_LIST_FILE}")

add_subdirectory(common)    # Util functions across data structures

add_subdirectory(list)      # Dynamic Array
add_subdirectory(llist)     # Linked List
add_subdirectory(stack)     # Stack
